{
  "project_id": "luqrrjhvaremronfcvaf",
  "query": "SELECT p.id, p.full_name, p.email, p.role_key, p.is_active, p.must_change_password, r.display_name AS role_name, r.permissions FROM tips_crm.profiles p LEFT JOIN tips_crm.roles r ON r.key = p.role_key ORDER BY p.created_at ASC LIMIT 50;"
}
