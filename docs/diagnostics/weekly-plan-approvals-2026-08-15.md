# تحقق خطط الاعتماد الأسبوعية — 15 أغسطس 2026

## المصدر

تم فحص مشروع Supabase `luqrrjhvaremronfcvaf` عبر موصل Supabase المفعّل للجلسة.

## النتائج

* وُجدت خطة أسبوعية مشتركة بحالة `pending` عند الفحص، مؤرخة من 22 إلى 28 أغسطس 2026.
* كان التطبيق ينشئ الخطة المشتركة، لكنه لم يكن يقرأ قائمة الخطط إلى بيانات المدير ولم يكن يحفظ صفوف `plan_visits`؛ لذلك لم تكن شاشة المدير المنفصلة قادرة على عرض الجدول التفصيلي من مصدر مشترك.
* طُبق ترحيل `tips_crm_plan_list_and_schedule` بنجاح. أضاف دالتي `public.tips_crm_list_plans()` و`public.tips_crm_save_plan_visits(uuid, jsonb)` وتأكد وجودهما بعد الترحيل.

## الأثر

أي خطة جديدة يرسلها المندوب تحفظ جدول زياراتها في `tips_crm.plan_visits` ثم تظهر للمدير في شاشة اعتماد الخطط الأسبوعية مع المندوب والأيام والجهات. الخطط التي أُنشئت قبل هذا التحسين قد تظهر دون تفاصيل زيارات، لأن صفوف جدول الزيارة لم تكن تُحفظ وقت إنشائها.
