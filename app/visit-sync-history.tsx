import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { router } from "expo-router";
import { FlatList, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import { AppHeader, palette } from "@/components/crm-ui";
import { ScreenContainer } from "@/components/screen-container";
import { useCrm } from "@/lib/crm-store";
import type { VisitSyncHistoryEntry } from "@/lib/offline-visit-drafts";

const formatTimestamp = (value: string) => new Date(value).toLocaleString("ar-SD", { day: "numeric", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit" });

export default function VisitSyncHistoryScreen() {
  const { accountById, offlineVisitSyncHistory } = useCrm();
  const syncedCount = offlineVisitSyncHistory.filter((entry) => entry.status === "synced").length;
  const failedCount = offlineVisitSyncHistory.filter((entry) => entry.status === "failed").length;

  return <ScreenContainer className="px-5" containerClassName="bg-background"><FlatList data={offlineVisitSyncHistory} keyExtractor={(item) => item.id} contentContainerStyle={[styles.content, !offlineVisitSyncHistory.length && styles.emptyContent]} showsVerticalScrollIndicator={false}
    ListHeaderComponent={<><AppHeader eyebrow="آخر 100 نتيجة مزامنة محفوظة على هذا الهاتف" title="سجل مزامنات التقارير" right={<TouchableOpacity onPress={() => router.back()} style={styles.back}><MaterialIcons name="close" size={21} color={palette.primary} /></TouchableOpacity>} /><View style={styles.summary}><View style={[styles.metric, styles.successMetric]}><MaterialIcons name="cloud-done" size={18} color={palette.success} /><Text style={styles.metricValue}>{syncedCount}</Text><Text style={styles.metricLabel}>تم الإرسال</Text></View><View style={[styles.metric, styles.failureMetric]}><MaterialIcons name="error-outline" size={18} color={palette.error} /><Text style={styles.metricValue}>{failedCount}</Text><Text style={styles.metricLabel}>احتاج إعادة محاولة</Text></View><View style={[styles.metric, styles.allMetric]}><MaterialIcons name="history" size={18} color="#246B91" /><Text style={styles.metricValue}>{offlineVisitSyncHistory.length}</Text><Text style={styles.metricLabel}>إجمالي السجل</Text></View></View>{offlineVisitSyncHistory.length ? <Text style={styles.listTitle}>تفاصيل المزامنات</Text> : null}</>}
    renderItem={({ item }: { item: VisitSyncHistoryEntry }) => { const account = accountById(item.accountId); const success = item.status === "synced"; return <View style={[styles.entry, success ? styles.successEntry : styles.failedEntry]}><View style={styles.entryTop}><View style={[styles.status, success ? styles.successStatus : styles.failedStatus]}><MaterialIcons name={success ? "check-circle" : "error-outline"} size={14} color={success ? palette.success : palette.error} /><Text style={[styles.statusText, success ? styles.successText : styles.failedText]}>{success ? "تم الإرسال" : "فشلت المحاولة"}</Text></View><Text style={styles.timestamp}>{formatTimestamp(item.recordedAt)}</Text></View><Text style={styles.accountName}>{account?.name ?? "جهة مرتبطة بزيارة"}</Text><Text style={styles.accountMeta}>{account ? `${account.type} · ${account.city}` : "تفاصيل الجهة غير متاحة حالياً"}</Text><View style={styles.detailGrid}><View style={styles.detail}><Text style={styles.detailLabel}>نتيجة الزيارة</Text><Text style={styles.detailValue}>{item.result}</Text></View><View style={styles.detail}><Text style={styles.detailLabel}>رقم المحاولة</Text><Text style={styles.detailValue}>{item.attempt}</Text></View></View>{item.noteExcerpt ? <Text numberOfLines={2} style={styles.note}>{item.noteExcerpt}</Text> : null}<View style={[styles.message, success ? styles.successMessage : styles.failedMessage]}><MaterialIcons name="info-outline" size={15} color={success ? palette.success : palette.error} /><Text style={[styles.messageText, success ? styles.successMessageText : styles.failedMessageText]}>{item.message}</Text></View></View>; }}
    ItemSeparatorComponent={() => <View style={{ height: 10 }} />}
    ListEmptyComponent={<View style={styles.empty}><View style={styles.emptyIcon}><MaterialIcons name="history" size={34} color="#246B91" /></View><Text style={styles.emptyTitle}>لا توجد مزامنات مسجلة بعد</Text><Text style={styles.emptyCopy}>عندما يُحفظ تقرير دون إنترنت ثم يُرسل لاحقاً، سيظهر هنا وقت الإرسال وحالة كل محاولة.</Text><TouchableOpacity onPress={() => router.replace("/(tabs)" as never)} style={styles.returnButton}><Text style={styles.returnText}>العودة لجدولي</Text><MaterialIcons name="arrow-forward" size={17} color="#FFFFFF" /></TouchableOpacity></View>}
  /></ScreenContainer>;
}

const styles = StyleSheet.create({ content: { paddingTop: 10, paddingBottom: 30 }, emptyContent: { flexGrow: 1 }, back: { width: 40, height: 40, borderRadius: 14, backgroundColor: "#E9F8F2", alignItems: "center", justifyContent: "center" }, summary: { flexDirection: "row-reverse", gap: 7, marginTop: 5 }, metric: { flex: 1, minHeight: 85, padding: 10, borderRadius: 15, borderWidth: 1, alignItems: "center", justifyContent: "center", gap: 3 }, successMetric: { backgroundColor: "#EFF8F4", borderColor: "#C7E6D9" }, failureMetric: { backgroundColor: "#FFF7F7", borderColor: "#F3C8C8" }, allMetric: { backgroundColor: "#F1F8FC", borderColor: "#CDE3EF" }, metricValue: { color: palette.ink, fontSize: 18, fontWeight: "900" }, metricLabel: { color: palette.muted, fontSize: 9, fontWeight: "800", textAlign: "center" }, listTitle: { color: palette.ink, fontSize: 14, fontWeight: "900", textAlign: "right", marginTop: 21, marginBottom: 9 }, entry: { borderWidth: 1, borderRadius: 18, padding: 13, backgroundColor: "#FFFFFF" }, successEntry: { borderColor: "#C7E6D9" }, failedEntry: { borderColor: "#F3C8C8" }, entryTop: { flexDirection: "row-reverse", justifyContent: "space-between", alignItems: "center", gap: 8 }, status: { paddingHorizontal: 8, paddingVertical: 5, borderRadius: 9, flexDirection: "row-reverse", alignItems: "center", gap: 4 }, successStatus: { backgroundColor: "#EAF8F2" }, failedStatus: { backgroundColor: "#FDEAEA" }, statusText: { fontSize: 10, fontWeight: "900" }, successText: { color: palette.success }, failedText: { color: palette.error }, timestamp: { color: palette.muted, fontSize: 10, textAlign: "left" }, accountName: { color: palette.ink, fontSize: 15, fontWeight: "900", textAlign: "right", marginTop: 11 }, accountMeta: { color: palette.muted, fontSize: 10, textAlign: "right", marginTop: 3 }, detailGrid: { flexDirection: "row-reverse", gap: 8, marginTop: 11 }, detail: { flex: 1, backgroundColor: "#F7FAF9", borderRadius: 10, padding: 8, alignItems: "flex-end" }, detailLabel: { color: palette.muted, fontSize: 9, fontWeight: "700" }, detailValue: { color: palette.primary, fontSize: 11, fontWeight: "900", marginTop: 3, textAlign: "right" }, note: { color: "#45534E", fontSize: 11, lineHeight: 17, textAlign: "right", marginTop: 10 }, message: { marginTop: 10, padding: 9, borderRadius: 11, flexDirection: "row-reverse", gap: 6, alignItems: "flex-start" }, successMessage: { backgroundColor: "#EFF8F4" }, failedMessage: { backgroundColor: "#FDEAEA" }, messageText: { flex: 1, fontSize: 10, lineHeight: 15, textAlign: "right" }, successMessageText: { color: "#176A50" }, failedMessageText: { color: "#A13A3A" }, empty: { flex: 1, alignItems: "center", justifyContent: "center", paddingHorizontal: 26, paddingBottom: 80 }, emptyIcon: { width: 70, height: 70, borderRadius: 25, backgroundColor: "#EAF2F8", alignItems: "center", justifyContent: "center" }, emptyTitle: { color: palette.ink, fontSize: 18, fontWeight: "900", textAlign: "center", marginTop: 15 }, emptyCopy: { color: palette.muted, fontSize: 12, lineHeight: 19, textAlign: "center", marginTop: 7 }, returnButton: { marginTop: 21, minHeight: 45, paddingHorizontal: 18, borderRadius: 14, backgroundColor: palette.primary, flexDirection: "row-reverse", alignItems: "center", justifyContent: "center", gap: 7 }, returnText: { color: "#FFFFFF", fontSize: 12, fontWeight: "900" } });
